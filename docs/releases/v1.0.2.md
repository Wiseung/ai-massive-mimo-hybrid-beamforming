# v1.0.2: Maintenance provenance alignment

## Scope

This is a maintenance patch release for the interface-first Sionna bridge.

It aligns the published maintenance state after `v1.0.1` with the latest provenance-refresh commit on `main`.

Included:

- RC provenance audit metadata refresh
- `current_head` metadata alignment
- post-`v1.0.1` maintenance state cleanup
- no functional or benchmark-scope changes

## Relationship to v1.0.1

`v1.0.1` remains a valid maintenance release for post-`v1.0.0` maintenance audits.

`v1.0.2` only captures the follow-up provenance metadata refresh that landed after the `v1.0.1` tag.

## What is not included

- No new models
- No new benchmark scope
- No Sionna RT
- No ray tracing
- No 5G NR full stack
- No full native-only benchmark claim
- No mainline native replacement claim
- No strict project_rzf equivalence claim
- No stable learned > WMMSE-iter5 claim
- Sionna remains optional dependency

## Validation

- compileall passed
- pytest passed
- maintenance audits remain valid
- release scope and boundary statements unchanged
