model:
  name: cnn
  hybrid: false
  condition_on_snr: true
  snr_embed_dim: 16
  base_channels: 32
  pool_factor: 2
  hidden_dims: [1024, 512]
  residual_to_mrt: true

training:
  epochs: 5
  batch_size: 128
  lr: 0.001
  weight_decay: 0.0
  seed: 42
  num_workers: 0
  lambda_power: 0.0
  lambda_const: 0.0
  amp: true
  val_fraction: 0.2
  selection_metric: val_loss
  selection_mode: min

evaluation:
  batch_size: 128
